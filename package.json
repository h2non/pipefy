{
  "name": "pipefy",
  "description": "Transform a function into a pipeable stream",
  "version": "0.1.1",
  "license": "MIT",
  "repository": "h2non/pipefy",
  "author": "Tomas Aparicio",
  "keywords": [
    "pipe",
    "streams",
    "stream",
    "writable",
    "transform",
    "proxy",
    "pipeable"
  ],
  "engines": {
    "node": ">= 0.10.0"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha --ui tdd"
  },
  "devDependencies": {
    "chai": "^1.9.1",
    "mocha": "^1.21.3"
  }
}
